apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: blockchain-ml
type: Opaque
stringData:
  POSTGRES_USER: blockchain_user
  POSTGRES_PASSWORD: "change-me-to-secure-password"
  DATABASE_URL: "postgresql://blockchain_user:change-me-to-secure-password@postgres:5432/blockchain_db"
